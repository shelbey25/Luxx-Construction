# Optional: point the frontend at a remote backend instead of the local Vite proxy.
# Leave this unset for local development against http://localhost:4000.
VITE_API_BASE_URL=https://server-production-5c2b.up.railway.app/api
